# CareerOS evals (M0-T19)

Offline evaluation harness for AI quality. The CI `eval-gate` job runs
`evals/run_gate.py`.

Today this is a **placeholder that always passes**. Real golden datasets, judges,
and fail-on-regression land in M3/M9 (see `docs/ai-architecture.md` and the M0
roadmap T19).
